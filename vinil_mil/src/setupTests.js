// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';
import App from './App'

// page paths
// '/src/pages/AllProductsPage/index.jsx',
//     '/src/pages/FinishOrderPage/index.jsx',
//     '/src/pages/HomePage/index.jsx',
//     '/src/pages/MenuAdminPage/index.jsx',
//     '/src/pages/ProfilePage/index.jsx',
//     '/src/pages/SearchResultsPage/index.jsx',
//     '/src/pages/ShoppingCartPage/index.jsx',
//     '/src/pages/UserAuthenticationPage/index.jsx'


import { render } from '@testing-library/react'

describe('', () => {
    it ('should be able to register as new user', () => {
        const {} = render(<App />)

        expect()
    })
})

describe('', () => {
    it ('should be able to add new product to the inventory as an admin', () => {
        const {} = render(<App />)

        expect()
    })
})

describe('', () => {
    it ('should be able to login as a user', () => {
        const {} = render(<App />)

        expect()
    })
})

describe('', () => {
    it ('should be able to login as an admin', () => {
        const {} = render(<App />)
 
        expect()
    })
})

describe('', () => {
    it ('should be able to add products to cart as a user', () => {
        const {} = render(<App />)

        expect()
    })
})

describe('', () => {
    it ('should be able to finalize an order as a user', () => {
        const {} = render(<App />)

        expect()
    })
})

describe('', () => {
    it ('should be able to search for a product', () => {
        const {} = render(<App />)

        expect()
    })
})

describe('', () => {
    it ('should be able to remove products as an admin', () => {
        const {} = render(<App />)

        expect()
    })
})

describe('', () => {
    it ('should be able to edit products as an admin', () => {
        const {} = render(<App />)

        expect()
    })
})